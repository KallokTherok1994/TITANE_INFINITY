/**
 * TITANE∞ v35.1.8 — Admin Center Page (router entry point)
 * Delegates to the unified Admin feature module.
 * TITANE∞ v35.1.8 — Admin Page Entry
 * Re-export du module ADMIN unifié vers le routeur principal (App.tsx)
 */
export { AdminPage as default } from '@/features/admin';
