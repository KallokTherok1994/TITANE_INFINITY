# BUILD ALL 2026-05-15 v35.1.6

- MODE: DURABLE
- Scope: bump version, rebuild frontend, build release Linux bundles, sync deployment/latest, refresh launchers/icons.

## Actions and Evidence

1. Version bump
- Command: `node scripts/bump-version.mjs && node scripts/sync-versions.mjs`
- Result: PASS, version moved `35.1.5 -> 35.1.6`.

2. Frontend rebuild
- Command: `corepack pnpm run build`
- Result: PASS (`vite build` success + post-build run).

3. Linux release build
- Command: `corepack pnpm run build:tauri`
- Result: PASS, bundles created:
  - `src-tauri/target/release/bundle/appimage/titane-infinity_35.1.6_amd64.AppImage`
  - `src-tauri/target/release/bundle/deb/titane-infinity_35.1.6_amd64.deb`
  - `src-tauri/target/release/bundle/rpm/titane-infinity-35.1.6-1.x86_64.rpm`

4. Android artifact gate
- Command: `corepack pnpm run android:artifact:check`
- Result: PASS (`apk_count=1`, `aab_count=1`).

5. deployment/latest sync
- Commands:
  - `bash scripts/post-build/update-deployment-latest.sh`
  - explicit cleanup legacy v35.1.1 artifacts
  - regenerate checksums/sizes/manifest for v35.1.6 set
- Result: PASS, `deployment/latest` normalized on v35.1.6 artifacts.

6. Desktop icons and launchers
- Commands:
  - `bash scripts/post-build/update-desktop-icons.sh`
  - `bash scripts/install-appimage.sh`
- Result: PARTIAL PASS (user-space sync and icon cache updated).
- Blocker:
  - system install and system launcher sync require sudo interactive password.
  - observed message: `sudo: il est necessaire de saisir un mot de passe`.

## Current Runtime Truth

- `dpkg -s titane-infinity` => `Version: 34.0.12`
- `~/.local/share/applications/titane-infinity.desktop` => `Exec=/usr/bin/titane-infinity`, `Icon=titane-infinity`
- `/usr/share/applications/titane-infinity.desktop` => `Exec=/usr/bin/titane-infinity`, `Icon=titane-infinity`
- local fallback binary exists: `~/.local/bin/titane-infinity-appimage` (v35.1.6 AppImage)

## Classification

- Session verdict: `BLOCKED_APPROVAL`
- Reason: privileged system upgrade step cannot complete without user-provided sudo authentication.

## Operator Next Step (<=30 min)

1. Run:
   - `sudo dpkg -i src-tauri/target/release/bundle/deb/titane-infinity_35.1.6_amd64.deb`
   - `bash scripts/post-build/update-desktop-icons.sh`
2. Re-verify:
   - `dpkg -s titane-infinity | rg '^Version:'`
   - `sed -n '1,40p' /usr/share/applications/titane-infinity.desktop`
   - `which -a titane-infinity`
