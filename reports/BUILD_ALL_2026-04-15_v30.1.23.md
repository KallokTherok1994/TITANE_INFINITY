# BUILD ALL — Report v30.1.23

Date: 2026-04-15T14:51:58Z
Verdict target: BLOCKED

## Commands

- `node scripts/bump-version.mjs`
- `source "$HOME/.nvm/nvm.sh" && nvm use && pnpm run tauri build`
- `source "$HOME/.nvm/nvm.sh" && nvm use && pnpm run android:build:full`
- `cp -f src-tauri/target/release/bundle/{appimage,deb,rpm}/*30.1.23* deployment/latest/`
- `cp -f src-tauri/target/release/titane-infinity deployment/latest/titane-infinity`
- `bash scripts/update-desktop-icon.sh`
- `dpkg -s titane-infinity`
- `source "$HOME/.nvm/nvm.sh" && nvm use && pnpm run android:artifact:check`
- `source "$HOME/.nvm/nvm.sh" && nvm use && pnpm run verify:tauri-configs`

## Evidence

- Canonical version bumped to `30.1.23` and synchronized across Cargo/Tauri/runtime manifests.
- Linux artifacts built successfully: AppImage, DEB, RPM.
- Android build completed successfully and reported `Finished 1 APK` plus `Finished 1 AAB`.
- `deployment/latest` now points to desktop 30.1.23 artifacts and includes the deployed binary hash `titane-infinity`.
- Local launcher regeneration completed and now writes `Name=TITANE∞ v30.1.23` with `Exec=/usr/bin/titane-infinity` in repo and user launchers.
- `dpkg -s titane-infinity` still reports `Version: 30.1.22`, proving the host install step did not complete in this non-interactive session.

## Residual Limits

- System package reinstall is blocked because `sudo dpkg -i ...` required an interactive password prompt.
- System launcher synchronization is blocked for the same reason because `bash scripts/post-build/update-desktop-icons.sh` escalates via `sudo`.
- Windows MSI was not produced locally because this governed session ran on a Linux host.
- The Android AAB finished successfully but retained the same observed SHA256 as the previous local run; this session records the real observed output rather than claiming a forced content delta.

## Final Validation

- `pnpm run verify:tauri-configs` passed.
- `pnpm run android:artifact:check` passed with unsigned-release honesty.
- `pnpm exec vitest run src/__tests__/config/desktopLauncherScripts.test.ts` passed.
- `bash scripts/autoheal/detect_recurrence.sh` passed.
- `bash scripts/verify_instructions.sh` passed.