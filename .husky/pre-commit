#!/bin/sh
# TITANE∞ — Husky v10 Universal Pre-commit Hook
# Correction PATH pour environnements limités (GitHub Desktop, Pop!_OS, Flatpak…)

. "$(dirname "$0")/_env"

# Exécution lint-staged via npm exec (plus fiable que npx)
npm exec lint-staged
