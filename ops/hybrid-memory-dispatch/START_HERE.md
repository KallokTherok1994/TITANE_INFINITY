# Start Here

1. Launch `scripts/dispatch/start-hybrid-memory-dispatch.sh`.
2. Open the generated run folder under `reports/agent_dispatch/<timestamp>/`.
3. Paste the generated commander prompt into the IDE session that will run `memory-root-commander`.
4. Use `OFFICIAL_HANDOFF_PLAN.md` as the authoritative charter for all handoffs.
5. Do not promote any phase without fresh proof and rollback evidence.