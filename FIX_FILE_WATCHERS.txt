╔══════════════════════════════════════════════════════════╗
║                                                          ║
║      ⚠️  FIX : ENOSPC File Watchers Limit               ║
║                                                          ║
╚══════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────┐
│  🔴 ERREUR DÉTECTÉE                                     │
└─────────────────────────────────────────────────────────┘

  Error: ENOSPC: System limit for number of file watchers reached

  Cause : Linux limite le nombre de fichiers surveillés
          (inotify watchers) à 8192 par défaut.

  Impact : Vite/npm ne peut pas démarrer le hot-reload.

┌─────────────────────────────────────────────────────────┐
│  ✅ SOLUTION PERMANENTE (Recommandée)                   │
└─────────────────────────────────────────────────────────┘

  Ouvrez un VRAI TERMINAL (GNOME Terminal/Konsole) :

  # Augmenter la limite à 524288
  echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf

  # Appliquer immédiatement
  sudo sysctl -p

  # Vérifier
  cat /proc/sys/fs/inotify/max_user_watches

  Résultat attendu : 524288

┌─────────────────────────────────────────────────────────┐
│  🔧 SOLUTION TEMPORAIRE (Session actuelle)              │
└─────────────────────────────────────────────────────────┘

  Terminal système (sans redémarrage requis) :

  sudo sysctl fs.inotify.max_user_watches=524288

  (Valeur perdue après reboot)

┌─────────────────────────────────────────────────────────┐
│  📊 VÉRIFICATION                                        │
└─────────────────────────────────────────────────────────┘

  Avant correction :
    cat /proc/sys/fs/inotify/max_user_watches
    → 8192 (insuffisant)

  Après correction :
    cat /proc/sys/fs/inotify/max_user_watches
    → 524288 (OK ✅)

┌─────────────────────────────────────────────────────────┐
│  🚀 RELANCER APRÈS CORRECTION                           │
└─────────────────────────────────────────────────────────┘

  cd ~/Documents/TITANE_NEWGEN/TITANE_INFINITY

  # Nettoyer
  lsof -t -i:5173 | xargs kill -9

  # Relancer
  npm run dev

  Résultat attendu :
    ✓ VITE v6.4.1 ready in 118ms
    ✓ Local: http://localhost:5173/

┌─────────────────────────────────────────────────────────┐
│  💡 POURQUOI CE PROBLÈME ?                              │
└─────────────────────────────────────────────────────────┘

  Vite/Webpack/npm watch utilisent inotify pour surveiller
  les modifications de fichiers en temps réel (hot reload).

  Votre système a beaucoup de fichiers dans :
    /home/titane_os/Bureau/RECUP/

  Cela consomme les watchers disponibles.

  Solutions :
    1. Augmenter la limite (recommandé)
    2. Nettoyer /home/titane_os/Bureau/RECUP/
    3. Exclure ce dossier de la surveillance

┌─────────────────────────────────────────────────────────┐
│  🧹 NETTOYAGE OPTIONNEL                                 │
└─────────────────────────────────────────────────────────┘

  Si /home/titane_os/Bureau/RECUP/ n'est plus nécessaire :

  # Vérifier la taille
  du -sh ~/Bureau/RECUP/

  # Supprimer si inutile
  rm -rf ~/Bureau/RECUP/

  Cela libérera les watchers.

┌─────────────────────────────────────────────────────────┐
│  ⚡ COMMANDE RAPIDE (Copy-Paste)                        │
└─────────────────────────────────────────────────────────┘

  Terminal système (GNOME Terminal) :

  echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p

  Puis dans VSCode :

  npm run dev

╔══════════════════════════════════════════════════════════╗
║                                                          ║
║  🎯 RÉSUMÉ                                               ║
║                                                          ║
║  1. Ouvrir terminal système                             ║
║  2. Exécuter : echo fs.inotify.max_user_watches=524288  ║
║                | sudo tee -a /etc/sysctl.conf           ║
║  3. Appliquer : sudo sysctl -p                          ║
║  4. Relancer : npm run dev                              ║
║                                                          ║
╚══════════════════════════════════════════════════════════╝
