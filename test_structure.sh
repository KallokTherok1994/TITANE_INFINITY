#!/usr/bin/env bash
set -euo pipefail

# ═══════════════════════════════════════════════════════════════════════════════
# 🔧 SCRIPT DE TEST - Validation Structure TITANE_INFINITY v12.0
# Test tous les chemins critiques du projet
# ═══════════════════════════════════════════════════════════════════════════════

echo "╔══════════════════════════════════════════════════════════════════════════════╗"
echo "║          🧪 TEST STRUCTURE TITANE_INFINITY v12.0                             ║"
echo "║                  Validation Complète des Chemins                             ║"
echo "╚══════════════════════════════════════════════════════════════════════════════╝"
echo

# Détection racine absolue
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$SCRIPT_DIR"

echo "📂 Racine du projet: $PROJECT_ROOT"
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 1 : src-tauri/ existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 1/10 : Dossier src-tauri/"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -d "$PROJECT_ROOT/src-tauri" ]]; then
    echo "✅ src-tauri/ existe"
else
    echo "❌ src-tauri/ manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 2 : Cargo.toml existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 2/10 : Cargo.toml"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/src-tauri/Cargo.toml" ]]; then
    echo "✅ Cargo.toml existe"
    echo "   Version: $(grep -m1 '^version' "$PROJECT_ROOT/src-tauri/Cargo.toml" | cut -d'"' -f2)"
else
    echo "❌ Cargo.toml manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 3 : tauri.conf.json existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 3/10 : tauri.conf.json"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/src-tauri/tauri.conf.json" ]]; then
    echo "✅ tauri.conf.json existe"
else
    echo "❌ tauri.conf.json manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 4 : main.rs existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 4/10 : main.rs"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/src-tauri/src/main.rs" ]]; then
    echo "✅ main.rs existe"
    HANDLERS=$(grep -c '#\[tauri::command\]' "$PROJECT_ROOT/src-tauri/src/main.rs" || echo "0")
    echo "   Handlers Tauri: $HANDLERS"
else
    echo "❌ main.rs manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 5 : package.json existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 5/10 : package.json"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/package.json" ]]; then
    echo "✅ package.json existe"
    echo "   Version: $(grep -m1 '"version"' "$PROJECT_ROOT/package.json" | cut -d'"' -f4)"
else
    echo "❌ package.json manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 6 : tsconfig.json existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 6/10 : tsconfig.json"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/tsconfig.json" ]]; then
    echo "✅ tsconfig.json existe"
else
    echo "❌ tsconfig.json manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 7 : vite.config.ts existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 7/10 : vite.config.ts"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -f "$PROJECT_ROOT/vite.config.ts" ]]; then
    echo "✅ vite.config.ts existe"
else
    echo "❌ vite.config.ts manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 8 : core/frontend/ existe
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 8/10 : core/frontend/"
echo "═══════════════════════════════════════════════════════════════════════════════"

if [[ -d "$PROJECT_ROOT/core/frontend" ]]; then
    echo "✅ core/frontend/ existe"
    TS_FILES=$(find "$PROJECT_ROOT/core/frontend" -name "*.tsx" -o -name "*.ts" | wc -l)
    echo "   Fichiers TypeScript: $TS_FILES"
else
    echo "❌ core/frontend/ manquant"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 9 : cargo metadata fonctionne
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 9/10 : cargo metadata"
echo "═══════════════════════════════════════════════════════════════════════════════"

cd "$PROJECT_ROOT/src-tauri" || exit 1

if cargo metadata --format-version 1 &>/dev/null; then
    echo "✅ cargo metadata fonctionne"
else
    echo "❌ cargo metadata échoue"
    exit 1
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# TEST 10 : Rust toolchain
# ═══════════════════════════════════════════════════════════════════════════════
echo "═══════════════════════════════════════════════════════════════════════════════"
echo " TEST 10/10 : Rust toolchain"
echo "═══════════════════════════════════════════════════════════════════════════════"

if command -v rustc &>/dev/null; then
    echo "✅ rustc: $(rustc --version)"
else
    echo "❌ rustc manquant"
    exit 1
fi

if command -v cargo &>/dev/null; then
    echo "✅ cargo: $(cargo --version)"
else
    echo "❌ cargo manquant"
    exit 1
fi

if command -v rustfmt &>/dev/null; then
    echo "✅ rustfmt: disponible"
else
    echo "⚠️  rustfmt manquant (optionnel)"
fi
echo

# ═══════════════════════════════════════════════════════════════════════════════
# RÉSUMÉ FINAL
# ═══════════════════════════════════════════════════════════════════════════════
echo "╔══════════════════════════════════════════════════════════════════════════════╗"
echo "║                           ✅ TOUS LES TESTS RÉUSSIS                          ║"
echo "╚══════════════════════════════════════════════════════════════════════════════╝"
echo
echo "📊 Résumé:"
echo "   ✅ Structure Tauri v2 complète"
echo "   ✅ Backend Rust opérationnel"
echo "   ✅ Frontend React/TypeScript opérationnel"
echo "   ✅ Outils de build disponibles"
echo
echo "🚀 Projet prêt pour:"
echo "   • npm run dev           (développement)"
echo "   • npm run build         (build frontend)"
echo "   • npm run tauri:build   (build production)"
echo
