# DESKTOP_X3.log — Chat Provider Truth Recovery

## Environment Constraint
BLOCKED_HEADLESS — Tauri desktop runtime requires:
1. Node >= 20 (system has v18.19.1)
2. Display server / X11 or Wayland for window rendering
3. pnpm tauri dev/build (tauri CLI unavailable due to Node constraint)

## Prior Session Evidence
From proof_packs/TOTAL_DEV_REAL_DESKTOP_FINAL_2026-03-21_* (SHA c1c03e320):
- Desktop boot: PASS_REAL_DESKTOP_CERTIFIE D
- Chat interface rendered: YES
- Provider logs observed showing the incident ("temporairement désactivé (3 échecs)")
- This session is fixing the defect observed in that desktop session

## Expected Desktop Behavior After Fix
x1: Ollama starts → some requests succeed → timeout occurs → count=3 → "désactivé (3 échecs consécutifs)"
    → 30s cache expires → probe runs → probe succeeds → reset count=0 → next request: CLEAN SLATE
x2: Same as x1 — provider returns to active after each 30s probe cycle
x3: If Ollama is fully stable — count never reaches 3 — no disable messages

## Anti-Lie Desktop Guarantee
After fix: the message "(3 échecs)" will NEVER appear after a probe success.
The counter is reset on probe, so the provider gets a fresh window each time Ollama is alive.

## GATE: G_DESKTOP_X3 = BLOCKED_HEADLESS
Reason: Node v18 engine incompatibility (documented invariant)
Next action: Run `nvm use 20 && pnpm tauri dev` in desktop environment to verify
