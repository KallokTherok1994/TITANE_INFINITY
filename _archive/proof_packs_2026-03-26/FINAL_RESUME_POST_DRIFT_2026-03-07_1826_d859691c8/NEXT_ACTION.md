# NEXT_ACTION

Fix the `scripts/tauri/before-dev.sh` pnpm path issue and the current pre-deployment blockers, then rerun only `scripts/verify/pre-deployment-check.sh --quick` and `scripts/verify/verify-preprod.sh` in a new bounded resume lane.

