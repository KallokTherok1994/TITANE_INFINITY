# CI Approval Report

Status: `BLOCKED`

Reason:

- Not reached because bootstrap fail-fast gate stopped execution.

Next action:

- After successful synchronization and technical gates run, fetch `action_required` workflows and classify `BLOCKED_APPROVAL` if manual approval is still required.
