# 09_STOP_LINE_DECISION

Stop-the-line trigger:
- Unresolved doctrine contradiction affecting the only remaining hygiene action space.

Trigger evidence:
1. `raw/docs_registry_instructions.md` + `raw/policy_markers_rg.txt` require append-only proof packs and no deletion.
2. `raw/lib_cert.sh` allows certification precheck with untracked proof outputs.
3. `raw/proof_pack_tracking_mode_sample.txt` demonstrates mixed repository practice (`tracked` and `untracked`).

Decision:
- Stop and classify `BLOCKED_DOCTRINE` until explicit policy authority defines one canonical tracking mode.
