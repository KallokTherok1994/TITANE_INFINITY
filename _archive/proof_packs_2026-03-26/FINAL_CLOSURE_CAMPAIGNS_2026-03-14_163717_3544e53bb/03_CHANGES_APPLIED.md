# 03 CHANGES APPLIED

## Applied fixes

1. N2 proof hardening (real-path vs degraded-path assertions).
2. UI proof hardening (backend/UI alignment with robust fallback).
3. G5 strict-mode counter fix.
4. run-all strict-mode counter fix.
5. G7 strict-mode counter fix.
6. WDIO -> wrapper env propagation bridge.

## Evidence

- Raw logs are stored under `raw/`.
- AutoHeal entries appended in `scripts/autoheal/autoheal_rules.jsonl`.
