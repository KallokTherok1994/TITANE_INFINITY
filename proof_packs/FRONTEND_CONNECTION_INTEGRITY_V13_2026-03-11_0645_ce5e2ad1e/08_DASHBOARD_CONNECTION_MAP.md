# 08 Dashboard Connection Map

Top-level route modules are linked into one router graph via `AppRouter` and surfaced via `TopNav`.

Sample mapped modules:

- Titane (`/titane`)
- Stats (`/stats`)
- Time (`/time`)
- Admin (`/admin`)
- Dev (`/dev`)
- Orchestration (`/orchestration-center`)

Proof:

- `raw/09_route_map_core.txt`
