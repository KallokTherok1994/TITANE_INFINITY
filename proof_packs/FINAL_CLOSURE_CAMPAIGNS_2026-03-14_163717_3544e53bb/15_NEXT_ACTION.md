# 15 NEXT ACTION

Single next action (<=30 min):

1. Run one targeted N2 real-provider campaign with controlled timeout (`TITANE_CONVERSATION_TIMEOUT_SECS=45`) until at least one non-degraded real-answer pass is captured, then re-run `run-all` and update verdict.
