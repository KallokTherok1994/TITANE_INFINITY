# V53 TARGET RUNTIME SURFACES SNAPSHOT
## Files to instrument/trace:

### 1. src/hooks/useConversationEngine.ts
import { useState, useCallback, useRef, useEffect, useMemo } from 'react';
import { logger } from '@/lib/logger';
import type {
import {
   [... full reconciliation in PHASE 1 ...]

### 2. src/hooks/useMemoryCore.ts
import { useState, useCallback, useMemo } from 'react';
import { tauriClient } from '@/lib/tauriClient';
import { memoryService } from '../services/api';
import type { MemoryEntry } from '../core/ARCHITECTURE_TYPES_v∞';
const toTimestampMs = (value: unknown): number => {
   [... full reconciliation in PHASE 1 ...]

### 3. src/services/conversationEngine.ts
import { tauriClient } from '@/lib/tauriClient';
import { validateIpcPayload } from '@/lib/ipcContract';
import { getSystemPrompt } from '@/config/chatModes.config';
import type {
import { FEATURE_FLAGS, envFlag } from '@/config/featureFlags';
import {
   [... full reconciliation in PHASE 1 ...]

### 4. src/services/chat/chatMemorySingleDoor.ts
import type { ConversationMode } from '@/services/conversationEngine';
import type { ProviderDecisionMeta } from '@/types/providerMeta';
import type { ModuleRouteContext } from '@/services/chat/moduleRouteContext';
const LAST_ENVELOPE_KEY = 'titane_chat_context_envelope_v1';
export interface ChatContextEnvelope {
   [... full reconciliation in PHASE 1 ...]

## Backend surfaces:
### conversation_generate command
   Location: src-tauri/src/**/conversation/generate.rs (to be located)
   IPC handler for: { systemPrompt, contextEnvelope, messages, ... }
