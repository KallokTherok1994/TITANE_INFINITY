# COMMANDS USED

- bootstrap truth (git/token masked)
- tests: `pnpm test:architecture` x3
- tests: `TITANE_E2E_TAURI=1 pnpm run test:e2e:vitest` x3
- build prod x3 with exact token env
- run release x3 online with `TITANE_PROBE_BOOT_MARKERS=1` and 90s timeout
