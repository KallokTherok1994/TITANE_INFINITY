# COMMANDS USED

- `git status --porcelain`
- `git rev-parse --short HEAD`
- `git log -20 --oneline`
- `git --no-pager diff --stat`
- masked token presence recording (`<present>`)
- `timeout 3600s pnpm run build:production` (x3)
- `timeout 60s src-tauri/target/release/bundle/appimage/TITANE-Infinity_27.2.0_amd64.AppImage` (x3)
- strict no-web scan via `rg` over UI scope
- `pnpm test:architecture` (x3)
- `TITANE_E2E_TAURI=1 pnpm run test:e2e:vitest` (x3)
- append-only diff check for `registry/ui-events.jsonl`
- `get_errors` on changed scope files
