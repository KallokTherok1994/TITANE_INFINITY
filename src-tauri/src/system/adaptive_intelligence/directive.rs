// TITANE∞ v8.0 - Adaptive Intelligence Engine: Directive Generation

pub fn build_adaptive_directive(plasticity: f64, adaptation: f64, reserve: f64) -> String {
    if reserve > 0.75 {
        return "Système prêt à absorber de nouvelles variations.".to_string();
    }
    if adaptation > 0.7 {
        return "Poursuivre l'ajustement dynamique actuel.".to_string();
    if plasticity < 0.4 {
        return "Renforcer la plasticité : approfondir l'insight et réduire la tension.".to_string();
    if reserve < 0.4 {
        return "Stabiliser avant d'adapter davantage.".to_string();
    "Adaptation progressive recommandée.".to_string()
}
