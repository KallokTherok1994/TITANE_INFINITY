// TITANE∞ v30.0.0 - Memory Bridge
pub struct MemoryBridge;

impl MemoryBridge {
    pub fn new() -> Self {
        Self
    }
}

impl Default for MemoryBridge {
    fn default() -> Self {
        Self::new()
    }
}
