// TITANE∞ v14.1 - Context Sync
pub struct ContextSync;

impl ContextSync {
    pub fn new() -> Self {
        Self
    }
}

impl Default for ContextSync {
    fn default() -> Self {
        Self::new()
    }
}
