# Prompt: Start Hybrid Memory Dispatch

> **Agent**: invoke memory-root-commander for this session.

## Mission

Launch the governed hybrid-memory dispatch program.

## Required outcomes

- keep the current memory system as the safe baseline
- sequence the work through orchestrator, masters, and specialists
- require proof before every phase transition
- stop the line on regression, integrity drift, or missing rollback
- report one final verdict using governed vocabulary only

## Starter brief

Use the handoff pack under `ops/hybrid-memory-dispatch/` as the execution charter, then delegate to `memory-orchestrator` to build the phase board and assign masters.