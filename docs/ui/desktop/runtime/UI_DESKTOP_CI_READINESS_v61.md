# UI_DESKTOP_CI_READINESS_v61

**Mission**: TITANE UI_DESKTOP_TIER1_BLOCKER_REDUCTION_AND_REMOTE_SYNC_v61  
**Date**: 2026-05-10  
**Schema**: v61  

---

## CI Readiness Assessment

### Static Gate Commands (CI-Safe, No Binary Required)

These commands run in CI without a Tauri binary:

| Command | CI Safe? | Notes |
|---|---|---|
| `pnpm run check` | ✅ YES | TypeScript + lint |
| `pnpm run verify:backend-proof-depth:strict` | ✅ YES | Verifier only reads JSONL |
| `pnpm run verify:instructions` | ✅ YES | Layer validation |
| `bash scripts/autoheal/detect_recurrence.sh` | ✅ YES | JSONL schema validation |
| `pnpm run guard:ipc-contract` | ✅ YES | Contract tests (42/42) |

### Desktop E2E Commands (NOT CI-Safe without binary)

| Command | CI Safe? | Notes |
|---|---|---|
| `WDIO_SPEC=... node scripts/e2e/run-desktop-suite.js` | ⚠️ REQUIRES binary | tauri-driver needs compiled binary |
| `TITANE_ENFORCE_BINARY_FRESHNESS=0 ... node scripts/e2e/run-desktop-suite.js` | ✅ For UI probes | No binary needed for browser context probes |

---

## v61 CI Impact

### New Spec Files

The 4 v61 spec files are gated by `TITANE_E2E_FULL=1`:
- `e2e/desktop/ui-desktop-tier1-blocker-reduction-agent-chat.wdio.test.js`
- `e2e/desktop/ui-desktop-tier1-blocker-reduction-experience.wdio.test.js`
- `e2e/desktop/ui-desktop-tier1-blocker-reduction-research.wdio.test.js`
- `e2e/desktop/ui-desktop-tier1-blocker-reduction-cloud.wdio.test.js`

These specs use `probeTier1BlockerReduction()` (UI evidence, no IPC) and can run with `TITANE_ENFORCE_BINARY_FRESHNESS=0`.

### Verifier Changes

`verify-backend-proof-depth.mjs` now accepts v61 schemaVersion. The v61 artifact is in ARTIFACTS_STATIC with WARN-only enforcement (missing = WARN, not FAIL). This means:
- CI passes even before v61 suite is run in CI (missing artifact = WARN)
- CI passes after v61 suite runs (artifact present = full validation)

---

## Recommended CI Workflow Addition

For a lightweight static gate that covers v61 changes:

```yaml
# .github/workflows/titane-static-gates.yml (excerpt)
- name: Static proof depth gate
  run: |
    TITANE_PROOF_ARTIFACT=artifacts/backend-proof-depth/v61-tier1-blocker-reduction.jsonl \
      pnpm run verify:backend-proof-depth:strict
  # exits 0 if artifact missing (WARN), exits 1 if artifact present and FAIL
```

---

## Current CI Baseline

| Gate | Status | Reference |
|---|---|---|
| pnpm run check | PASS (EXIT:0) | Pre-v61 baseline |
| guard:ipc-contract | PASS (42/42) | Pre-v61 baseline |
| verify:backend-proof-depth:strict (v60) | PASS | Committed artifact |
| verify:backend-proof-depth:strict (v61) | PASS | v61 artifact created |
| detect_recurrence.sh | PASS | Post-AutoHeal |

---

_Generated by v61 mission — TITANE_INFINITY v33.0.11_
