# UI Desktop Strict Backend Proof — Next Actions v60

**Date**: 2026-05-10
**Status**: v60 gate CERTIFIED ✅

---

## Immediate Next Actions (CI Readiness)

### 1. Integrate `verify:backend-proof-depth:strict` into CI pipeline

Add to `.github/workflows/`:
```yaml
- name: Verify backend proof depth (strict v60)
  env:
    TITANE_PROOF_ARTIFACT: artifacts/backend-proof-depth/v60-strict-backend-proof.jsonl
  run: pnpm run verify:backend-proof-depth:strict
```

Requires: v60 artifact pre-generated by E2E suite run in CI.

### 2. PROOF_DEPTH_BLOCKED_BY_RUNTIME → IPC proven (25 records)

For the 25 blocked-at-runtime records, the long-term target is to prove IPC in a headless Tauri environment. This requires:
- A dedicated Tauri E2E runner with Ollama fully initialized
- Or: mock IPC commands that always return predictable responses

Target: reduce `PROOF_DEPTH_BLOCKED_BY_RUNTIME` from 25 to < 10 in v61.

### 3. Legacy artifact v58 source spec back-fill (optional)

If v58 WARNs need to be resolved, add `sourceSpec: "LEGACY_V58"` to all v58 records via a migration script. Not required for v60 gate PASS.

### 4. v61 target: Tier 1 uiEvidence for all 12 modules

All 12 Tier 1 modules currently have `UI_REFLECTS_BACKEND_RESULT` or BLOCKED. Target: 100% `UI_REFLECTS_BACKEND_RESULT` with `uiEvidence.found: true` for all Tier 1 modules in v61.

---

## Gate Status Summary

| Gate | v60 Status |
|---|---|
| `verify:backend-proof-depth:strict` | ✅ PASS (FAIL:0) |
| `detect_recurrence.sh` | ✅ PASS (1775 entries) |
| `verify_instructions.sh` | ✅ PASS=52 FAIL=0 |
| WDIO suite code | ✅ 0 |
| v60 artifact records | ✅ 53 |
| All sourceSpec | ✅ 100% |
| All schemaVersion v60 | ✅ 100% |
