# COMMANDS RUN — ONLINE-FINAL (append-only)

## Phase 0 — Baseline
```bash
git status -sb
git rev-parse HEAD
git branch --show-current
node -v
cargo -V
```

## Phase 1 — G1 Inventory
```bash
grep -rn --include="*.ts" --include="*.tsx" --include="*.rs" --include="*.sh" --include="*.json" \
  "OFFLINE|offline|MODE HORS LIGNE|LOCAL_FIRST|local-first|local_only|LOCAL_ONLY|FORCE_LOCAL|NETWORK|network_used|provider_used|REMOTE|REMOTE_ONLY|LOCAL_MODE|127\.0\.0\.1|localhost|proxy|/api/" \
  src/ src-tauri/src/ scripts/ \
  > docs/_evidence/online_final/G1_INVENTORY_RAW.txt 2>/dev/null
```

## Phase 2 — G2 Network Surface
```bash
grep -rn "reqwest::\|Client::new\|https\?://" src-tauri/src/ --include="*.rs" > /tmp/be_network.txt
grep -rn "fetch(" src/ --include="*.ts" --include="*.tsx" > /tmp/fe_network.txt
```

## Phase 3 — G3 Meta Sources
```bash
grep -rn "mode.*REMOTE|provider_used|network_used|reason_code|POLICY_BLOCKED|local_only" src/ src-tauri/src/
```

## Phase 4 — G4 Network Check
Verified: `src-tauri/src/ai/router.rs` — `check_internet()` using reqwest GET to google.com (3s timeout)

## Phase 5 — G5 Tests x3
```bash
NODE_OPTIONS='--max-old-space-size=4096 --require ./tests/polyfills/resizable-arraybuffer.cjs' \
  ./node_modules/.bin/vitest run src/__tests__/provider-decision-invariants.test.ts src/__tests__/online-availability.test.ts
```

## Phase 6 — G6 Runtime Proof
BLOCKED — cannot launch Tauri binary in this environment.

## Phase 7 — SHA256
```bash
( cd docs/_evidence/online_final && find . -maxdepth 2 -type f -print0 | sort -z | xargs -0 sha256sum ) > docs/_evidence/online_final/SHA256SUMS.txt
```
