# PROVIDERS & KEYS

## Fallback Garanti
- External AI OFF → Local/mock + explication.
- External ON + clé invalide → Local/mock + erreur recoverable.
- External ON + clé valide → Provider choisi.

## UI Governance
- Bouton "Tester Provider" (sans loguer clé).
- Validation format + statut.

## Test
Missing key → Chat répond quand même.