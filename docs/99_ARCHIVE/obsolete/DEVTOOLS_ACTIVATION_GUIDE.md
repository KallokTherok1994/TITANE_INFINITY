# 🔧 DevTools Activation Guide - TITANE∞ v15.5

## ✅ DevTools Successfully Activated

All modifications have been applied to enable DevTools in Tauri v2.

---

## 🎯 What Was Changed

### 1. **tauri.conf.json**
- ✅ Added `"label": "main"` to window configuration
- ✅ Added `"devtools": true` to enable DevTools
- ✅ Relaxed CSP to allow `'unsafe-inline'` for debugging
- ✅ Added WebSocket support for hot-reload (`ws://localhost:*`)

### 2. **src-tauri/src/main.rs**
- ✅ Added `.setup()` hook to open DevTools automatically in debug mode
- ✅ DevTools open on app start with `#[cfg(debug_assertions)]`
- ✅ Added logging for DevTools state
- ✅ Window label verification ("main")

### 3. **src/main.tsx**
- ✅ Added keyboard shortcuts: **F12** and **Ctrl+Shift+I**
- ✅ DevTools can be opened from anywhere in the app
- ✅ Error handling with proper TypeScript types

### 4. **index.html**
- ✅ Added fallback debug button (visible even if React crashes)
- ✅ Fixed position, red background, always on top (z-index: 99999)
- ✅ Works even if JavaScript/React fails to load

---

## 🚀 How to Use DevTools

### Method 1: Automatic (Debug Build)
```bash
npm run tauri:dev
```
**→ DevTools open automatically** ✅

### Method 2: Keyboard Shortcuts
Once the app is running:
- Press **F12**
- Or press **Ctrl+Shift+I**

### Method 3: Emergency Fallback Button
If the UI is black and nothing works:
- Look for the **red "🔧 DEBUG" button** in top-right corner
- Click it to force-open DevTools

---

## 🧪 Testing Instructions

### Test 1: Launch in Dev Mode
```bash
cd ~/Documents/TITANE_NEWGEN/TITANE_INFINITY
npm run tauri:dev
```

**Expected:**
- Window opens
- DevTools panel appears automatically
- Console shows: `🔧 DevTools opened automatically (debug mode).`
- Console shows: `🔧 DevTools shortcuts enabled: F12 or Ctrl+Shift+I`

### Test 2: Test Keyboard Shortcuts
Once the app is running:
1. Press **F12** → DevTools should open/close
2. Press **Ctrl+Shift+I** → DevTools should open/close

### Test 3: Test Fallback Button
1. Look for red button in top-right corner
2. Click it
3. DevTools should open

### Test 4: Check Console for Errors
In DevTools Console tab, look for:
- React errors
- Failed imports
- Network errors
- WebView errors

---

## 🔍 Debugging Black Screen

With DevTools now active, follow these steps:

### Step 1: Check Console Tab
Look for:
- ❌ `Failed to load module`
- ❌ `Uncaught TypeError`
- ❌ `404 Not Found`
- ❌ CSP violations

### Step 2: Check Network Tab
Verify:
- ✅ `main.tsx` loads successfully
- ✅ `titane-v12.css` loads
- ✅ All imports return 200 OK

### Step 3: Check Elements Tab
Verify:
- ✅ `<div id="root">` exists
- ✅ React components are mounted
- ✅ CSS is applied

### Step 4: Check Sources Tab
Verify:
- ✅ Source maps are loaded
- ✅ You can see TypeScript/JSX code
- ✅ Breakpoints can be set

---

## 🚨 Common Black Screen Causes

| Issue | Solution |
|-------|----------|
| **CSP too restrictive** | ✅ Already relaxed in tauri.conf.json |
| **Missing dist files** | Run `npm run build` |
| **Wrong distDir path** | Already set to `../dist` |
| **React error on mount** | Check ErrorBoundary in console |
| **Missing CSS** | Check Network tab for 404s |
| **WebView crash** | Check `journalctl -xe \| grep titane` |

---

## 📊 Files Modified

```
✅ src-tauri/tauri.conf.json    (devtools + label + CSP)
✅ src-tauri/src/main.rs         (setup hook + auto-open)
✅ src/main.tsx                  (keyboard shortcuts)
✅ index.html                    (fallback button)
```

---

## 🎯 Next Steps

1. **Install system dependencies first** (if not already done):
   ```bash
   bash install_system_deps.sh
   ```

2. **Launch in dev mode**:
   ```bash
   npm run tauri:dev
   ```

3. **Check DevTools console** for errors

4. **Report what you see** in the console

---

## 🔥 Production Build

To enable DevTools in production build, modify `main.rs`:

```rust
// Remove #[cfg(debug_assertions)] to enable in production
if let Some(window) = app.get_webview_window("main") {
    window.open_devtools();
}
```

⚠️ **Not recommended for final release** (security risk)

---

## ✅ Summary

**DevTools are now fully activated with:**
- ✅ Automatic opening in debug mode
- ✅ F12 keyboard shortcut
- ✅ Ctrl+Shift+I keyboard shortcut  
- ✅ Emergency fallback button
- ✅ Relaxed CSP for debugging

**You can now debug the black screen issue!**

---

*Generated by TITANE∞ DevTools Activator v2.1 | 2025-11-20*
