# NEXT_ACTIONS

Action unique (prioritaire):

**Migrer tous les appels réseau frontend (`globalThis['fetch']`, `@tauri-apps/plugin-http`) vers une porte backend IPC unique, puis rerun complet des gates (`G_UI_NO_NETWORK_DIRECT`, `G_ONE_DOOR_NETWORK_BACKEND`, `G_SELF_AUDIT_CLEAN`, `G_BUILD_TAURI_X3`).**
